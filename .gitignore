# AI assistant local state (never tracked)
.agent/
.claude/
.qorelogic/
CLAUDE.md
GEMINI.md
COPILOT.md
CURSOR.md

# NOTE: docs/, plan-*.md, and .failsafe/ are deliberately TRACKED.
# Governance, provenance, and planning artifacts are part of this
# repository's public record — see README "Intent & Value Provenance".

# Experiment run state (live, machine-generated; archives live in data/runs/)
experiment_log.txt
experiments.tsv
experiments.checkpoint.json
*.checkpoint.json
experiments/*.tsv

# IDE local settings
.vscode/
.idea/
.cursor/
.windsurf/

# Build artifacts
target/
*.swp
*.swo
*~

# Environment
.env
.env.*
*.pem
*.key

# OS
Thumbs.db
.DS_Store
