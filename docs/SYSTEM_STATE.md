# System State

**Regenerated**: 2026-08-11
**Regenerated By**: Repository review session (branch `claude/agent-memory-repo-review-2tay4p`)
**Supersedes**: the 2026-04-06 seal (session `9adc8319`), which had drifted two sessions behind reality — it referenced `docs/ARCHITECTURE_PLAN.md` and `docs/CONCEPT.md` (absent from the repo), described the pre-rename `codegenome-core/` layout, and listed security blockers S3/S4 as open after BACKLOG and META_LEDGER Entry #118 marked them resolved.

## Workspace (Reality)

```text
CODEGENOME/
|-- Cargo.toml                  workspace: identity, substrate, cli, mcp
|-- codegenome-identity/        graph, UOR identity, overlays, lang backends,
|                               index pipeline, fusion, signal, evidence, diff
|-- codegenome-substrate/       experiments, belief, embedding, federation,
|                               governance (policy, ledger, write gate)
|-- codegenome-cli/             11 commands (thin clap dispatch)
|-- codegenome-mcp/             MCP stdio server, 11 tools, write gate
|-- docs/                       governance + planning record (tracked, public)
|-- data/runs/                  archived experiment raw data (RUN-001)
`-- .failsafe/governance/       latest audit verdict
```

`codegenome-core/` was deleted 2026-08-11 (Shadow Genome Failure #6 — it was a byte-identical orphan of `codegenome-substrate/` left by the Session 5 rename; seal #118's rename claim was corrected).

## Metrics (verified 2026-08-11)

| Metric | Value |
|--------|-------|
| Tracked Rust source files | 198 |
| Total lines of Rust | ~15,300 |
| Tests | 264 passing (`cargo test --workspace`) |
| Clippy warnings | 0 (`cargo clippy --workspace --all-targets`) |
| Overlays | 9 registered (8 active; LSP overlay is a stub) |
| Edge types | 16 |
| CLI commands | 11 |
| MCP tools | 11 (7 read, 4 write-gated) |
| Governance ledger entries | 118 (+ Shadow Genome #6 correction) |

## Open Items

- **S2 / B15** — capability broker (blocked on FailSafe-Pro) is the only open security blocker.
- Wishlist: W1 (IDE panels), W4 (SLSA attestation), W5 (Sigstore signing).
- Known correctness gaps under repair (see `docs/PRODUCT_REVIEW_2026-08-11.md` §3): symbol addressing omits file path; Rust backend is non-recursive; experiment engine fitness switching inert.

## Notes

- The META_LEDGER genesis hash references `CONCEPT.md` + `ARCHITECTURE_PLAN.md`, which are not in the repo; the chain is not verifiable from a fresh clone until they are restored or the genesis is re-anchored.
- The three `docs/plan-*.md` files are retained as historical record of VETOed blueprints; each now carries a superseded banner.
- Repository has no tags; versions cited in BACKLOG (through v0.21.0) are documentation-level only. All crate manifests are 0.1.0.
